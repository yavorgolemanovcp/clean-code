//Low Cohesion and High Coupling
class UserManager {
  userStats: any;
  userProfile: any;
  
  public createUser() {
      // Create a new user
  }

  public calculateUserStatistics() {
      // Calculate and return some statistics about the user
  }

  public deleteUser() {
      // Delete a user
  }
}
